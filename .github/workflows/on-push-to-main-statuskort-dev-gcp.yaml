name: Deploy public statuskort-topic i dev-gcp

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - dev-gcp/aapen-statuskort.yaml

jobs:
  build-and-publish-on-master:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:

      - name: Sjekk ut koden
        uses: actions/checkout@v3

      - name: 'Deploy-er til min-side i dev-gcp'
        uses: 'nais/deploy/actions/deploy@v2'
        env:
          CLUSTER: dev-gcp
          RESOURCE: ./dev-gcp/aapen-statuskort.yaml
          PRINT_PAYLOAD: true
